dependencies:
  - role: nginx 

  - role: php5-fpm
    php_version: "5.6"
    max_execution_time: 300
    max_input_time: 300
    post_max_size: 16M
    always_populate_raw_post_data: -1

  - role: mariadb-server
    when: "zabbix_mysql_host == 'localhost'"
